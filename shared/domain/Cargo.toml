[package]
name = "domain"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
rust_decimal = "1.30"
rust_decimal_macros = "1.30"
uuid = { version = "1.4", features = ["v4", "serde"] }
