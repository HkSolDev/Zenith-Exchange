[package]
name = "db"
version = "0.1.0"
edition = "2021"

[dependencies]
domain = { workspace= true }
sqlx = {workspace = true} 
tokio = {workspace = true} 
rust_decimal = {workspace = true}
uuid = {workspace = true} 
dotenvy = {workspace = true}
